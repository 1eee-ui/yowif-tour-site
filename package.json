{
  "name": "yowif-server",
  "version": "1.0.0",
  "private": true,
  "description": "YoWif site with a real (local) backend: contact form, newsletter signup, admin panel. Data stored in JSON files, no native compilation required.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "pg": "^8.23.0"
  }
}
